{
  "buildCommand": "cd \"$(git rev-parse --show-toplevel)\" && echo \"=== DIAG: repo root (pwd) ===\" && pwd && echo \"=== DIAG: contents of repo root ===\" && ls -la && echo \"=== DIAG: contents of backend/ BEFORE build ===\" && ls -la backend && npm install --prefix frontend && npm run build --prefix frontend && echo \"=== DIAG: contents of frontend/build after CRA build ===\" && ls -la frontend/build && rm -rf build backend/build && cp -r frontend/build build && cp -r frontend/build backend/build && echo \"=== DIAG: repo root AFTER copy (looking for build/) ===\" && ls -la && echo \"=== DIAG: backend/ AFTER copy (looking for backend/build/) ===\" && ls -la backend",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
