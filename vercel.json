{
  "buildCommand": "cd .. && npm install --prefix frontend && npm run build --prefix frontend",
  "outputDirectory": "../frontend/build",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
